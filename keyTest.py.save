import threading
import sys
import select
import time

def check():
	i, o, e = select.select([sys.stdin], [], [], 0.0001)
	for s in i:
		if s == sys.stdin:
			input = sys.stdin.readline()
			print 'Input:' + input

while True:
	print check()
